version: '3.8'

services:
  web-videojuegos-final:
    image: nodensx/jenkins:latest
    ports:
      - "5009:5000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - red-videojuegos-nueva # Debe coincidir con la creada arriba
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

networks:
  red-videojuegos-nueva:
    external: true
