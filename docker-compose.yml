version: '3.8'
services:
  web-videojuegos:
    image: nodensx/videojuegos-app:latest
    ports:
      - "5009:5000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - red-videojuegos-nueva
    deploy:
      replicas: 1
networks:
  red-videojuegos-nueva:
    external: true
